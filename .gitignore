build/
.vs/
.vscode/
.claude/
*.user
*.suo
compile_commands.json

# CMake FetchContent (under build tree)
_deps/

# OS
Thumbs.db
Desktop.ini

# Python bytecode cache (e.g. vendor/artcade-runtime/tools)
__pycache__/
*.pyc

# Local test capture noise
script_test_out.txt
*_test_out.txt

# Vendored runtime (real copy since 2026-07-25; formerly a junction into
# ArtCade-Studio_V2\runtime-cpp). Build dirs stay out.
vendor/artcade-runtime/build*/

# Local WASM/native distribution output (not part of the editor source tree)
vendor/dist/

# ADR-0019: shipping player binary (refreshed via scripts/refresh-export-templates.bat)
src/editor-native/resources/export-templates/**/game.exe

# Phase-4 visual regression artefacts (references are committed; the editable
# visual-fixture project is local test data)
tests/reference/*.actual.png
tests/reference/*.diff.png
ui-capture.png
tests/reference/visual-fixture/visual-fixture.artcade
