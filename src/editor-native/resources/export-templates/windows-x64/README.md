# Export templates (ADR-0019)
#
# Refresh with: scripts\refresh-export-templates.bat
# Requires a Release (or local) runtime build that emits game.exe +
# runtime-build-info.json under vendor/artcade-runtime/build-native/src/app/.
#
# game.exe is not committed — see .gitignore.
